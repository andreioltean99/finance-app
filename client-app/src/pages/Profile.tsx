import {useContext, useEffect} from "react";

const Profile = () => {
    // const {user} = useContext(AuthContext);

    return (
        <div>
            User Profile -
        </div>
    )
}

export default Profile;